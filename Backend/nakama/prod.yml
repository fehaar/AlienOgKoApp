name: nakama1

logger:
  level: INFO

console:
  port: 7351
  # Username and password are set via NAKAMA_CONSOLE_USERNAME / NAKAMA_CONSOLE_PASSWORD env vars
  # or passed as --console.username / --console.password CLI flags

socket:
  port: 7350

api:
  port: 7349

session:
  token_expiry_sec: 7200

runtime:
  path: /nakama/data/modules
